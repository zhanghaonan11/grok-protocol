# Local Turnstile token factory deps (dedicated solver service)
# Keep this separate from the root browser/register requirements.

# HTTP API
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.7.0

# Browser runtime. Keep this range pinned to the fresh-context API used by the pool.
DrissionPage==4.1.1.4
psutil==7.2.2

# Shared with parent project when integrating:
# curl_cffi
